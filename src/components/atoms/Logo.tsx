import styled from '@emotion/styled'

const SLogo = styled.h2`
  font-size: 24px;
  font-weight: bold;
  color: #000;
  letter-spacing: 1px;
`

export const Logo = () => {
  return <SLogo>Exclusive</SLogo>
}
